{
  "name": "generic-quest",
  "version": "1.0.0",
  "description": "Generic Quest - A themeable turn-based JRPG",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:variants": "node scripts/build-variants.mjs",
    "build:app": "node scripts/build-app.mjs",
    "preview": "vite preview",
    "cap:sync": "npm run build:app && npx cap sync",
    "cap:android": "npm run build:app && npx cap sync && npx cap open android",
    "cap:ios": "npm run build:app && npx cap sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.0.0"
  }
}
