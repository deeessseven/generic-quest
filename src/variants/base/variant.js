// The base game: no extra scenes, no route overrides. This is the current Generic Quest, unchanged.
export default {
  id: 'base',
  scenes: [],   // extra Phaser.Scene classes registered on top of the core scenes
  routes: {},   // transition-point overrides (none = default flow)
};
