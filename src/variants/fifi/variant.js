// fifi — FiFi's edition. Same engine + base flow as the base game (NO celebration scenes). Differs
// only by FiFi's text (variants/fifi/gametext.txt) and FiFi's 3 hero sprites
// (variants/fifi/custom-art/). No extra scenes, no route overrides → plays exactly like the base.
export default {
  id: 'fifi',
  scenes: [],
  routes: {},
};
